# The main pipeline for pyspark
resources:
  pipelines:
    pyspark_pipeline:
      name: pyspark_pipeline
      catalog: workspace
      schema: pyspark_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
