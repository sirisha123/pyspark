# This is a Databricks asset bundle definition for pyspark.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: pyspark
  uuid: 5f23a313-8cf2-4a63-a5a9-8ceeba3a1aef

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-d50d651b-5409.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-d50d651b-5409.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/sirishareddykasipathi@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/sirishareddykasipathi@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: sirishareddykasipathi@gmail.com
        level: CAN_MANAGE
